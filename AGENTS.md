# AGENTS.md

## Source of truth

- 제품 범위와 품질 기준은 `PRD.md`가 기준이다.
- 현재 활성 마일스톤은 PRD의 R0이다.
- R0 밖의 기능은 구현하지 않고 후속 이슈로 기록한다.
- 연결 트랙은 PRD의 R6 선행조건을 통과하기 전까지 실제 장치 I/O를 구현하지 않는다.

## Architecture rules

- AI가 수치 결과나 판정을 생성하지 못하게 한다.
- 계산은 승인된 명세 버전만 입력으로 받는다.
- 모든 물리량은 값, 단위, 차원, 출처를 가진다.
- 실행 상태와 공학 판정 상태를 분리한다.
- 실행 전 preflight 거부와 실행 중 실패를 분리한다.
- 승인된 명세와 완료된 실행 기록은 불변으로 취급한다.
- 스키마, 엔진, 플러그인, 데이터 버전을 manifest에 기록한다.
- 미래 기능을 위한 빈 추상화나 디렉터리를 미리 만들지 않는다.
- 기존 유틸리티와 단순한 경계를 우선하고 새 의존성은 필요성을 설명한다.

## Safety and security

- 서버는 기본적으로 `127.0.0.1`에만 바인딩한다.
- 사용자 입력을 코드나 셸 명령으로 실행하지 않는다.
- 프로젝트 경로 밖 파일 접근과 경로 탈출을 거부한다.
- 비밀정보를 코드, DB, 로그, 테스트 fixture에 저장하지 않는다.
- 사용자·LLM 텍스트는 안전한 DOM API 또는 문맥별 인코딩으로 출력하고 CSP를 유지한다.
- 정적 서버는 public allowlist만 제공하며 소스·DB·로그·내부 절대경로를 노출하지 않는다.
- 필수 입력이 없거나 모델 적용 범위를 벗어나면 추정으로 강행하지 않는다.
- 외부 네트워크와 외부 프로세스 실행은 R0 범위가 아니다.
- AI에서 raw 장치 protocol 또는 adapter 호출로 이어지는 경로를 만들지 않는다.
- 장치 관련 schema를 추가하더라도 분석 판정과 실행 승인을 합치지 않는다.

## Development workflow

1. 변경 전 PRD의 해당 요구사항과 완료 조건을 확인한다.
2. 실패하는 테스트 또는 계약 테스트로 동작을 먼저 고정한다.
3. 통과에 필요한 최소 구현만 추가한다.
4. 포맷, lint, 타입 검사, 단위·통합 테스트를 실행한다.
5. 사용자 흐름이 바뀌면 최소 smoke test를 실행한다.
6. 문서와 실제 명령이 다르면 같은 변경에서 수정한다.

## Required commands

R0 골격을 만들 때 다음 명령을 하나의 루트 인터페이스로 제공한다.

- `dev`: 현재 활성 R0 절편의 로컬 실행. 웹·API 절편 전에는 계약 demo를 실행할 수 있음
- `test`: 단위 및 통합 테스트
- `lint`: 정적 검사
- `typecheck`: Python과 TypeScript 타입 검사
- `verify`: 포맷 검사, lint, typecheck, test 전체 실행

도구는 달라도 명령의 역할과 한 번의 전체 검증 진입점은 유지한다.

## Test requirements

- 스키마 호환성과 직렬화 왕복 테스트
- 필수 입력 누락·잘못된 단위·범위 이탈 테스트
- 판정 집계 조합 테스트
- 승인되지 않은 명세 실행 거부 테스트
- preflight 거부 시 AnalysisRun 없이 INDETERMINATE 근거가 생성되는 테스트
- 같은 manifest 재실행 테스트
- API 입력, 오류 상태, Content-Type, idempotency, Origin, CSRF, 경로 탈출 테스트
- 저장·반사·DOM XSS, CSP, 정적 파일 allowlist, 내부 경로 비노출 테스트
- 테스트 산출물의 임시 디렉터리 격리와 `verify` 전후 clean-tree 테스트
- 핵심 브라우저 E2E와 자동 접근성 테스트
- R0 가짜 플러그인의 결정론성 테스트

## Done criteria

- 변경 범위의 acceptance criteria가 테스트로 증명된다.
- `verify`가 새 환경에서 통과한다.
- 실행 방법과 실제 명령이 일치한다.
- 생성 파일, 로컬 DB, 비밀정보가 저장소에 포함되지 않는다.
- 핵심 코어 branch coverage 90% 이상이며 critical/high 공급망 취약점은 0건 또는 예외가 기록된다.
- 실패하거나 실행하지 못한 검증은 최종 보고에 명시한다.
- R0 밖 기능이 섞이지 않는다.
